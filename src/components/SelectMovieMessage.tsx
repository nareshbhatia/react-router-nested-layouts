import { Card, CardHeader, CardTitle } from './ui/card';

export function SelectMovieMessage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Select a movie</CardTitle>
      </CardHeader>
    </Card>
  );
}
